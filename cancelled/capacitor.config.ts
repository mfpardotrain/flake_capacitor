import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flake.out',
  appName: 'cancelled',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
